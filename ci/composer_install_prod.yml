---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: composer/composer

inputs:
  - name: github-repository

outputs:
  - name: build-dir

params:
  SOURCE: github-repository
  DESTINATION: build-dir
  MODE: production

run:
  path: composer_install.sh
